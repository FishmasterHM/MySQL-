SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
LIMIT
LIKE 一般和通配连用 % _(单个字符)
REGEXP 正则匹配
BINARY 区分大小写 (中文)
CONCAT() 连接字符串

DISTINCT
DESC
ASC
IN
AS     不返回客户机

INNER JOIN
LEFT(RIGHT) OUTER JION 外部联结
ON

UNION 连接SELECT语句

Match() Against()
中Match()指定被搜索的列,Against() 指定要使用的搜索表达式
WITH QUERY EXPANSION